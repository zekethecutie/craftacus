{
  "name": "craftacus",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Craftacus, the Craftein Universe Discord bot for a safe Minecraft Bedrock SMP community.",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/state.js && node --check src/content.js && node --check src/discord.js",
    "test": "node --test"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5",
    "minecraft-server-util": "^5.2.0"
  }
}
